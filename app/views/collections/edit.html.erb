<% content_for :title, "Editing collection" %>
<div class="M_BackTitle">
  <%= link_to "<- Назад", request.referer || posts_path, class: "A_BackLink" %>
</div>
<div class="L_Form">
  <h3>Редактирование коллекции</h3>
  <%= render "form", collection: collection_path(@collection) %>
</div>